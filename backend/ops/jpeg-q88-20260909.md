# JPEG Q88交付与验收 — 2026-09-09

## 结果
- 用户指定JPEG Q88；不裁切、不缩放，保留原件。93张高清封面均为Q88，194张小素材保留较小的无损优化或原样复制版本。
- 高清：50,096,189 → 33,002,793 bytes，减34.1%。全部287份：54,564,002 → 37,415,846 bytes，减31.4%。
- 本地及服务器均保留原件、WebP试验件、Q92试验件、Q88交付件。只发布Q88映射。

## 修改范围
- `backend/scripts/build_cover_jpeg.py`：生成独立版本，原件不变，拒绝覆盖不同的已发布内容。
- `backend/app/services/activity_cover_service.py`：JPEG交付清单映射，显式开关，哈希URL；不改变服务端原件解析。
- `backend/tests/test_cover_jpeg_delivery.py`：7项测试，包括287组SHA256、解码、尺寸、体积、开关、CDN兼容、原件解析、构建防覆盖。
- 服务器只基于实际运行版本增加JPEG映射；未携带本地无关CDN或其它脏工作区改动。
- 生产开关：`/etc/systemd/system/dragonreserve-backend.service.d/30-cover-jpeg-q88.conf`，`ACTIVITY_COVER_JPEG_ENABLED=1`。
- 生产原服务备份：`/home/ubuntu/apps/dragonReserveSystem/backend/ops/activity_cover_service.before-jpeg-q88-20260909.py`。
- 生产部署候选及脚本：`/tmp/dragon-prod-jpeg-q88-candidate.py`、`/tmp/deploy-dragon-q88.py`。脚本有旧代码哈希校验、备份、验证失败回滚。

## 验收证据
- 原JPEG与WebP同图对照：前者downloadFile=200且getImageInfo成功，后者200但getImageInfo:fail invalid。该结论仅限当前模拟器链路，不代表所有真机均不支持WebP。
- Q88全部287素材在微信模拟器以并发2完成真实downloadFile与getImageInfo验证，287成功、0失败。
- 实际首页11卡片均ready，0 error，15个图片/玻璃素材成功。
- 后端切换后刷新模拟器、实际重新拉取活动列表，11卡片正常，15/15素材来自 `http://usr/home-image-cache-v1/`。
- 本地缓存索引共15,920,315 bytes，Q88条目15；不是全93封面被首页预加载。
- 公网93封面记录及287个素材HEAD均200、image/jpeg，Content-Length总和37,415,846 bytes。
- 7项后端专项测试、323项前端Node测试通过；git diff --check通过。未运行完整后端pytest。
- 截图：`/tmp/dragon-jpeg-q88-home.jpg`、`/tmp/dragon-jpeg-q88-online-cache.jpg`。
- 公网核验：`/tmp/dragon-q88-public-validation.json`；本地测试：`/tmp/dragon-jpeg-q88-tests.txt`、`/tmp/dragon-jpeg-q88-front-tests.txt`。

## 边界与回滚
- 本次无需重发前端即可获取新图片URL；既有客户端刷新列表后生效。此前100MB缓存功能是否在线取决于对应前端版本，不能由此推断已发布。
- 没有新增埋点或改变并发、动画逻辑；未改数据库、未提交或推送Git。
- 本轮未做iOS/Android真机测试；压缩减少下载量，不承诺解决网络建连和弱网全部问题。
- 回滚可将独立systemd配置中开关设为0，daemon-reload并重启；或恢复已备份服务。保留旧URL及素材，避免破坏缓存用户。
