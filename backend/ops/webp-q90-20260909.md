# WebP Q90 发布尝试：素材完成，取图切换已回滚

参数 cwebp -q 90 -m 6 -sharp_yuv，无缩放。93原图50,096,189字节→26,707,342字节；包含缩略图、玻璃图、画师头像287文件54,564,002→30,998,418字节。缩略图合计反而增大，非全部文件均减重。

服务器原件全部SHA256一致，287WebP均解码成功且宽高一致。公网287 HEAD返回200 image/webp。服务器上同时保留两套。

切换后模拟器首页失败。独立wx.downloadFile成功200，随后wx.getImageInfo(tempFilePath)返回getImageInfo:fail invalid。不可推断所有真机WebP不可用；明确现有准备链路在模拟器不兼容。

已恢复服务器原activity_cover_service.py并重启。公网再次确认93 image_url全部JPEG。压缩素材保留，未删除。曾拉到WebP列表的客户端可能保留该列表，需刷新获取恢复后的JPEG链接；不能声称所有用户立刻恢复。

本地候选切换以ACTIVITY_COVER_WEBP_ENABLED=1显式启用，默认关闭，避免未完成客户端兼容修复就再次切换。下一步先解决并验证WebP准备/原生渲染，前端由用户发布，然后才能启用WebP。不得绕过失败直接视为已解码。
