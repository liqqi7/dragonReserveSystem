# 首页慢加载：2026-09-09 16:48 后续核验

## 新线上证据（正式版3.0.4，未包含本次本地修复）

按 diagnosticEventId 去重，以 occurredAt >= 2026-09-09 16:30:00 +08:00 筛选，排除 devtools；读取截至服务器16:48:35的应用日志，联结同请求的Caddy脱敏日志和ASGI日志。

### 16:37 iPhone 16 / iOS 18.5

- trace `home-view-1788943039751-cd52c918`：8.84秒隐藏时仍11张pending；并未达到15秒软超时。
- 1092159B原图 `hm-mttuhvtz-1-oufozllg8o`：手机最后上报180224B，随后取消；Caddy7.983秒输出458752B，ASGI34.83ms交付。
- 1001033B原图 `hm-mttuhvu4-3-d78t6sl0r8`：手机最后上报180224B，随后取消；Caddy7.923秒输出294912B，ASGI3.81ms交付。
- 30803B玻璃图 `hm-mttuhvu3-2-pebcz1wp6o`：手机准备3005ms，其中profile正文1888ms；Caddy29.9ms，ASGI27.31ms。
- 下一trace `home-view-1788943051776-cbf253bd` 重新请求尚未完成的图，5.472秒又隐藏取消，仍11张pending。
- 结论：正文接收缓慢也出现在iOS，且发生于软超时之前；15秒任务叠加不是所有慢下载的初始原因。最后进度不是取消瞬间精确接收量，网关输出量不是客户端确认量。

### 16:43 iPhone 17 Pro Max / iOS 26.6.1

trace `home-view-1788943418150-06223ba3`，12.26秒全部卡片ready状态提交；不是所有原生图片视图都已有loaded回调，也不以此证明每个屏外像素已绘制。

请求 `hm-mttupzs2-1-xgz3v5zs4a` 的SDK profile：

| 阶段 | 毫秒 |
|---|---:|
| SDK queue | 0 |
| DNS（domainLookUpEnd-start） | 3914 |
| connect（包含TLS区间，不与TLS相加） | 4544 |
| TLS | 4446 |
| fetchStart到responseStart | 9417 |
| 响应正文 | 2707 |

同请求Caddy正文处理2.555秒，ASGI38.34ms；其余最初两张图也在约9.4–9.6秒才收到响应头。后续图片多数准备约0.1–1秒。

结论：这个样本最主要的等待在收到响应正文之前，SDK记录的DNS和TLS区间均偏长；不能统一说成“图片太大导致正文下载慢”，也不能把它归为业务接口执行9秒。SDK时间区间仍不足以指定某个运营商、DNS服务商或服务器出口承担责任。

## 已完成的本地修复与验收

- 图片队列软超时15秒仅记录诊断，保留原槽和在途任务；手动重试不替换仍loading的任务。
- 正常成功或失败才释放槽；硬期限先请求取消再释放。迟到回调幂等，不重复显示或重试。
- 复核此前本地的隐藏pause/返回resume、进度停滞恢复、有限重试。
- 312项前端测试通过；包含队列+下载器组合测试：三张持续有字节进度的图片等待40秒仍只有三个任务，隐藏完成后返回复用，没有取消重下。
- 硬期限覆盖同步取消回调、延迟回调、取消抛异常，以及释放槽前取消顺序。
- 开发者工具刷新后截图：`/tmp/dragon-home-slots-20260909.jpg`，大卡/小卡/底部Tab正常可见。
- 运行时读取：joined 4/4、accepting 2/2、ended 5/5 ready，共11张，15个就绪文件，失败0。
- 15项后端运维脚本测试通过（TCP采样8项、离线关联分析7项）。

## 可复用的现有数据分析工具

`analyze-home-media.py`离线合并应用和网关日志，按请求输出DNS/TLS/正文区间、最后字节进度、完成/取消状态及ASGI/Caddy数据。排除开发者工具、去重、按发生时间筛选，不输出用户身份、IP、原始URL或认证信息。不需要增加前端埋点。

原始输入：`/tmp/dragon-followup-20260909-1650.log`、`/tmp/dragon-followup-gateway-1650.jsonl`。
脱敏关联结果：`/tmp/dragon-followup-joined-1650.json`。

## 未完成与边界

- 本轮没有上传、提交审核或发布前端，也没有部署新的后端业务代码；不能宣称线上已使用修复版。
- 这些代码修复消除任务叠加和隐藏重下，但不修复DNS/TLS或公网正文传输吞吐本身。
- 硬期限/停滞恢复后的原生abort是尽力取消；若SDK不回调或取消异常，不能称为全局严格TCP并发3。
- 没有这些故障时刻、精确关联请求的TCP历史指标。不能用16:25–16:28闲置连接采样替代故障现场，不能声称已确定具体网络责任方。
