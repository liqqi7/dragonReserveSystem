# 项目协作规则

## 环境职责：本地开发项目

1. 当前项目是 `dragonReserveSystem` 的本地开发环境，主要负责需求分析、功能开发、缺陷修复、自动化测试、代码审查和提交代码。
2. 所有功能性代码改动应优先在本项目完成并验证，不把本地目录当作线上运行环境，也不在这里假设线上服务状态。
3. 涉及上线、服务重启、线上日志、生产配置、数据迁移、备份或回滚时，应明确区分本地验证与线上操作；除非用户明确要求，不连接或修改线上服务器。
4. 开发完成后应说明可部署的提交或变更范围、验证结果、部署注意事项和回滚要点，供“线上运维项目”执行上线。
5. 不在代码、日志、提交或回复中泄露 `.env`、密钥、令牌、数据库凭据等敏感信息。

## 需求实施与验证

1. 收到需求后，先明确该需求的验证方式和验收标准。
2. 完成代码或配置修改后，必须由 Codex 自行执行一轮测试，不要求用户代为执行。
3. 测试方式根据需求选择，包括但不限于：界面操作验证、接口测试、pytest、脚本检查或其他自动化测试。
4. 测试完成后，向用户说明测试方式、实际结果，以及尚未覆盖的风险或需要用户在真实设备上确认的部分。


## 任务完成后的自检

1. 完成任务后，必须重新对照用户本轮需求、上下文中的相关描述、既有规范和验收标准，逐项执行一遍自检，不得仅凭工具返回成功判断任务完成。
2. 自检必须覆盖本次修改直接影响的内容及其关联内容，重点检查是否存在遗漏、错位、溢出、裁切、重叠、尺寸或间距不一致、样式不统一、旧内容残留、组件与实例不同步等异常。
3. 涉及 Pencil 或 `.pen` 原型时，必须检查目标节点、父容器及代表性页面；核对位置、宽高、对齐、内外边距、圆角、阴影、字号、颜色、层级、裁切和组件复用，并通过截图或其他可验证方式确认视觉结果。
4. 自检发现问题时应继续修正并重新检查；只有相关检查全部通过后，才能向用户报告完成。

## Pencil 原型规范与全局检查

### 统一规范来源

1. 现行规范为 [统一设计规范](prototype/design-system/README.md) 与 [统一设计值](prototype/design-system/tokens.json)，依据用户于 2026-09-13 上传的《龙城小程序.pen》更新。附件规范快照及冲突取舍已保存在该目录。
2. 主色使用 `#FF9800`，文字层级使用 `#111827`、`#4B5563`、`#9CA3AF`；页面和内容卡为白色，次级区域和一级抽屉为 `#F5F5F5`。
3. 移动端字号只使用 10、12、14、16、20、24、32px；一级页面标题为 24/34，分区与二级页面标题为 20/28。不要继续使用旧补全稿中的蓝色主题和 13、15、18px 临时字号。
4. 附件中的探索页不自动成为统一规范；规范说明与正式组件矛盾时，按统一规范文档记录的取舍执行。原型采用整数像素；合法的行高倍率和 px 到 rpx 转换不受此限制。
5. 桌游 UI 以现有小程序正式设计为基准，按 [页面参照与复用要求](prototype/design-system/miniprogram-ui-reference.md) 延续导航、封面卡、头像名单、表单和排行结构；BG Stats 用于参考业务能力。现有代码的旧视觉取值不覆盖现行规范，颜色统一不能代替页面结构与交互验收。

### 画布排列与验收

1. 每次修改 `.pen` 画布后，必须检查整个画布的全部顶层节点排列，不得只检查本次修改的局部区域。
2. 检查内容至少包括：全画布截图、所有顶层节点的坐标与边界、节点间遮挡或重叠、异常大空档、功能分组顺序及分区间距。
3. 顶层页面应按功能模块分行排列；同一功能横向排列，不同功能纵向排列；素材区、组件区、验证页面及业务页面应分区清晰且保持合理间距。
4. 新增或移动页面、组件、弹窗、抽屉、浮层后，必须重新执行全画布碰撞检查，并确认没有挡住既有画布。
5. 发现排列异常时必须先修正并重新检查；只有全画布无重叠、无异常空档、分组和间距符合规范后，才能报告完成。
6. 组件模板库必须采用一级、二级分类：一级分类固定为“基础规范、通用组件、业务组件”，二级分类再按色彩、圆角、阴影、导航、操作栏、表单、弹层、活动、日历等职责拆分；一个二级分类框只承载同一类内容，不把多个平级章节混装在同一个框内。新增规范优先并入对应二级分类，不为单个控件、单个状态或一次需求重复新增同级章节。

### 移动端弹层

1. 普通信息、新建和编辑使用统一一级抽屉；日期时间 Picker 保持独立结构，不自行增加规范没有的副标题和操作按钮。
2. 一级抽屉面板为 `#F5F5F5`、内容模块为 `#FFFFFF`、顶部圆角 `24px 24px 0 0`。顶部依次为 8px 留白、12px 拖拽区、8px 间距、40px 标题区、8px 间距，共 76px；拖拽条为 `38 × 4px`、`#9CA3AF`；标题 20px，右侧统一使用 `32 × 32px` 关闭入口。
3. 基准 390×844px 下，一级抽屉只能使用低 360、标准 558、高 720px 三档高度，底部对齐；信息内容视窗分别为 284、482、644px。超出时只滚动内容或升级一档。信息水平边距 16px，表单 18px；表单有提交操作时从内容视窗扣除 80px 白色固定操作栏并避开安全区，无操作的信息抽屉不加空底栏。
4. 日期、时间和日期时间选择器使用独立 Picker View：整体底色 `#FFFFFF`，标准高 396px，顶部圆角 `24px 24px 0 0`，标题栏高 `56px`、水平内边距 `20px`，右侧关闭区域 `32 × 32px`，不使用拖拽条；标题栏下方分隔线和选中行底色使用 `#F5F5F5`，选中行圆角 `12px`，底部固定 `80px` 白色确认栏。
5. 弹层遮罩覆盖完整移动端视口。普通抽屉沿用既有约 40% 黑色遮罩；选择器叠加在表单抽屉上时保留下层状态并增加第二层遮罩。不得为单个页面随意新增近似遮罩色。
6. 当前画布没有稳定的居中确认弹窗规范；遇到删除、退出或风险确认等场景时，先确认或补齐统一组件，不得自行推导样式。

## 日程页渲染问题的修复纪律

1. 日程页涉及原生 `swiper`、WXS、吸顶标题、周条高亮或“今天”列时，必须先从最近的已知基线版本开始，不得在上一次失败修复的工作区上继续叠加修改。
2. 每轮修复前必须保留当前失败状态，并将代码恢复到已知基线；若用户尚未授权恢复或需要保留现场，先只做只读分析，不得直接改代码。
3. 先在基线版本上复现并记录“未操作进入页面”的初始状态，再记录第一次滑动状态；不得把初始化错位、拖动错位和松手后错位混为同一个问题。
4. 在没有日志、截图或运行时证据证明原生组件语义前，不得假设 `current`、`display-multiple-items`、WXS `dx` 或列宽的含义；每次只验证一个假设。
5. 单元测试、语法检查和静态 diff 只能证明代码没有明显语法问题，不能代替 iOS 真机或微信开发者工具的渲染验收；涉及原生渲染时必须明确区分自动化结果和真实设备结果。
6. 每轮只允许修改解决当前根因所必需的最小范围；若验证失败，先回到同一个基线，再尝试下一种方案，不得在失败方案上继续修补。
7. 未经用户确认修复有效，不得删除诊断日志、提交版本或扩展修改到周条、今天列、活动数据及其他页面。
