# 活动分享卡片预生成实施计划

## 目标与验收口径

以 `prototype/龙城小程序.pen` 节点 `I2GgZO` 为唯一视觉基准。创建活动时生成可用的静态分享图；编辑活动的地点、地址、开始/结束时间或封面时，发布对应的新图。点击详情页“分享”时不发起图片生成，也不因生成状态禁用按钮。

验收：550 × 440 的静态图与原型的封面裁切、底部毛玻璃、地点/时间文案和图标布局一致；创建成功的活动立即有图片 URL；相关编辑成功后 URL 指向新图；无关编辑及报名变化不改图；连续打开详情、点击分享均不触发生成；GIF 分享图为预先选取的中间时刻静帧，而不是第一帧或动图。

## 原型核对结果

- 画布 550 × 440，封面填满画布、居中裁切；底部信息层从约 y=384 延伸至底边，背景模糊半径 20，并叠加黑色约 20%→30% 渐变。
- 信息层内边距纵向 12、横向 24；地点组、时间组之间间距 24；组内图标与文字间距 10。地点图标为白色 map-pin，时间图标为白色 clock-3，均为 22 × 22；文案白色，Noto Sans SC，地点 22、时间 20。
- 示例内容为“龙城运动中心”和“2026/12/31 19:30-20:00”。图中**没有**头像、报名人数或其他实时信息。实际时间文案应由开始/结束时间格式化；跨日时须完整表达结束日期，不能误写为同日。
- 原型使用 Pencil 图标；落地时采用项目要求的圆形 SVG 图标规范制作/复用对应 SVG，并用同一 SVG 资源栅格化到服务端图片中，避免两端图标不一致。若原型的 map-pin/clock-3 外形与既有圆形 SVG 不一致，先以原型外形为准制作 SVG，不用旧 PNG 替代。
- 长地点和长时间文案要根据实际字体宽度计算、留足图标/间距；制定省略策略并截图验证，不允许超出 550px 边界。

## 现状与改动范围

- `backend/app/services/activity_share_preview_service.py` 现以地点、人数/参与者及 `updated_at` 参与缓存键，绘制报名人数及可选头像；`Image.open(...).convert('RGBA')` 对 GIF 取首帧。现有 GET `/api/v2/activities/{id}/share-preview` 在读取时调用 `get_or_create_activity_share_preview`，不是纯读。
- V2 创建在 `backend/app/api/v2/activities.py` 内提交；V1 创建、V1/V2 编辑经过 `backend/app/services/activity_service.py`。必须覆盖两套写入口，避免旧入口改完后分享图过期。
- `miniprogram/pages/activity_detail/activity_detail.js` 在显示详情时另行请求分享图；WXML 在 `sharePreviewLoading` 时禁用分享按钮。`miniprogram/utils/shareActivity.js` 在缺图时可能回退至原封面，GIF 回退仍可能取首帧。

## 实施步骤

1. **重做纯静态渲染器。** 移除人数、头像、远程头像依赖；只使用本地封面、地点名称、开始/结束时间。按原型实现裁切、模糊、渐变、字体、图标及溢出策略。GIF 按累计帧时长选总时长约 50% 处的已合成帧，正确处理 disposal/透明帧；输出 RGB PNG。PNG/JPEG 等静态封面直接输出静态图。生成文件用临时路径写入并校验后原子替换，不在公开路径留下半张图片。
2. **持久化并按字段触发。** 给活动增加可空的分享图文件标识/版本字段（迁移），对外由后端构造 HTTPS 版本化 URL。签名只取卡片相关字段：封面 ID 与素材版本、地点名称、地址、开始与结束时间、布局版本；地址即使不直接写在卡片上，变化仍按需求触发。无关字段更新及报名不触发。创建取得 ID 后生成；编辑在锁定活动、验证变更后生成。文件准备成功才提交数据库；失败回滚并清除新临时/孤儿文件，返回明确错误，不让“创建/编辑成功但图不可用”。编辑期间旧图仍可用，提交成功后再延迟清理旧版本；不要提前删除旧图。多实例部署需确认 `media_root/share-previews` 是共享持久存储，否则迁移到共享对象存储。
3. **读接口和客户端解耦。** 详情响应直接带已持久化的分享图 URL（或在保留的 GET 接口纯读取），不得在 GET、页面打开或 `onShareAppMessage` 中生成。删除 `sharePreviewLoading` 对分享按钮的门禁；`open-type="share"` 保持可点，回调立即返回已取得的 URL。缺失图片时不回退至原 GIF：允许微信默认卡片图、记录可观测错误，并在后台补偿；异常路径不应伪装为预生成成功。分享图 URL 的内容版本变化要能绕过旧缓存；说明网络下载仍由微信执行，承诺的是“不等待服务端制图”，不是离线零延迟。
4. **迁移与上线顺序。** 先上数据迁移与渲染器，批量回填已有活动（支持断点续跑、失败清单和重试），核对每个有效活动文件存在、可访问、尺寸正确；再切换只读接口/小程序去掉等待。旧文件仅在确认新图、历史 URL 过渡期和备份策略后按引用清理；活动删除时关联清理。按批次测量同步创建/编辑耗时，避免封面异常尺寸或 GIF 解码拖慢写请求。
5. **验证。** 后端单测覆盖创建立即可用、地点名称/地址/开始/结束时间/封面变化、无关字段/报名不变化、GIF 中点帧、跨日/长文案、生成失败回滚与并发更新、旧数据回填、GET 不写文件。接口集成测试检查 V1/V2 创建编辑及 HTTPS URL；小程序测试检查分享按钮无禁用、回调不等待、缺图不回退 GIF。与原型截图对照尺寸和图层，运行 Skyline 对应 Skill 与 `skyline-cli wxss check`（若修改 WXSS/WXML）；最后用微信开发者工具及真机验证卡片实际展示和缓存更新，自动化通过不能替代该视觉验收。

## 取舍与风险

选择**创建/相关编辑同步预生成**，将可能的制图耗时前移到保存操作，换取分享路径无生成等待与写入成功后的图片可用保证。失败时保存失败、原活动及原图保留；若实测写入耗时不可接受，再单独评估队列方案，但异步生成需要“保存成功但分享图暂不可用”的产品状态，不能未经确认直接改为异步。文件与数据库并非单个原子事务，必须采用先完整文件、后提交引用、失败补偿、延迟清理的顺序，并为崩溃造成的孤儿文件提供巡检清理。

## 本轮执行记录与上线清单

### 已实现（本地代码）

- 服务端改为 550 × 440 静态 PNG：选中的本地封面居中裁切；GIF 按累计时长抽取约 50% 处的合成帧；信息层只绘制地点和完整时间，采用与原型对应的 SVG 图标。文件先写临时路径、校验后原子替换；缓存文件损坏会重制。
- 活动记录新增 `share_preview_file`，迁移 `20260924_0020`。V1/V2 创建同步生成，V1/V2 相关编辑通过字段签名切换版本 URL；生成失败返回 `SHARE_PREVIEW_GENERATION_FAILED`（503），数据库回滚，不让保存成功但图片缺失。提交失败清理新生成文件，历史 URL 暂保留。
- V2 详情直接返回图片 URL；V1/V2 的分享图 GET 只读取，不在请求时制图。小程序点击分享不等待制图、不因制图状态禁用；无图时交给微信默认分享卡片，不回退原 GIF。
- 新增 `backend/scripts/backfill_activity_share_previews.py`（默认干跑；支持 `--apply`、`--start-after`、`--limit`）和 `backend/scripts/prune_activity_share_previews.py`（默认干跑，至少保留 7 天）。目前**未对线上数据库或服务器文件执行**这两份脚本。

### 本地验证与仍需确认

- 与 Pencil `I2GgZO` 的同一张封面（`patryk-wojciechowicz-006`）生成并目视对照：550 × 440、居中裁切、底栏 y=384、两组图文位置与原型相符。参考和本地样图位于 `.codex-tmp/share-preview-reference/`，仅作为本地 QA，不参与线上部署。
- 针对性后端测试覆盖 V1/V2 创建、编辑字段变化、无关变更、只读 GET、GIF 中点、跨年时间与长地点、生成/提交失败回滚、损坏缓存重制、回填干跑和重试。迁移链通过 `alembic heads` 检查。
- 微信开发者工具及真机分享卡片实际展示、图片远程可达性、缓存切换与创建/编辑保存耗时**尚未验证**；静态检查和单测不能代替这些验收。

### 生产发布顺序（需另行确认后执行）

1. **备份**生产数据库及 `backend/storage`；确认 `.env` 的 `MEDIA_ROOT` 指向持久化目录（多实例必须共享），部署机已安装 CairoSVG 及其 Cairo 原生依赖，服务账户有 `share-previews/` 写权限，`PUBLIC_BASE_URL` 产生 HTTPS 地址。不要把本地的测试图或临时目录发布到服务器。
2. 停止或隔离写入流量，先上迁移 `20260924_0020`，再部署含新服务端代码及 `requirements.txt` 的版本。部署前先确认全部历史活动所选封面在新封面目录可用。生产环境执行命令时以 `backend/` 为工作目录。
3. 先干跑 `python -m scripts.backfill_activity_share_previews --limit 100`；确认后分批 `python -m scripts.backfill_activity_share_previews --apply --start-after <上一批最后的活动ID> --limit 100`。任何失败 ID 要在修复原因后重试，不跳过。检查所有现存活动的引用文件存在、PNG 550 × 440 可读取，并抽查公网 HTTPS URL 的真实访问与缓存头。
4. 仅在回填核对完成后发布去掉等待的小程序包；用管理员账号测创建、地点/地址/开始及结束时间/封面编辑、无关编辑，以及微信真机发送和接收分享卡片。记录保存耗时，GIF 大文件要单独测；若过慢，再另议异步方案，不能自行牺牲“创建即有图”。
5. 旧版本图片可能已被分享卡片引用，不要发布当天就删除。确认历史卡片保留窗口及备份后，先干跑 `python -m scripts.prune_activity_share_previews --min-age-days 14`，审阅清单再加 `--apply`。线上文件清理与数据库回滚都需单独授权。
