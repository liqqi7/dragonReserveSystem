# 活动封面分类目录（2026-09-23 分类快照；2026-09-25 清理更新）

- 分类标签：派对、运动、外出、游戏、电影、生日、吃饭、杂项；弃用
- 分类时图片总数：120（历史快照）
- 可选封面：44
- 分类时弃用封面：76（已于 2026-09-25 清理）

| 分类 | 数量 |
| --- | ---: |
| 派对 | 6 |
| 运动 | 5 |
| 外出 | 10 |
| 游戏 | 3 |
| 电影 | 6 |
| 生日 | 2 |
| 吃饭 | 6 |
| 杂项 | 6 |
| 弃用 | 76 |

正式分类数据位于 `backend/app/assets/activity-covers/catalog.json`。可用 `docs/activity-cover-classifier.html` 查看、调整和导出分类文本或 catalog JSON；工作台内的修改仅保存在浏览器 LocalStorage，需将导出的结果回传后再同步到正式目录。

## 历史封面替换同步

正式库最新只读审计曾发现 17 种弃用封面被 34 条历史活动记录使用；17 项替代映射已同步到工作台，并通过 Alembic `20260923_0019` 完成线上活动记录迁移。迁移后这 17 个旧封面 ID 已无活动引用；旧资源当时暂时保留以便回滚，已在下述清理中移除。

## 2026-09-25 线上清理结果

- 清理前再次核对：线上目录 120 张，其中弃用 76 张、可选 44 张；线上 57 条活动对弃用封面的引用为 0。
- 从线上和本地目录移除 76 张弃用封面及 3 个仅归属于弃用封面的作者头像；同时清理 3 个压缩清单里对应的派生文件。精确清理 228 个源文件、660 个压缩版本，共 888 个文件、135,408,897 字节；目录现在为 44 张可选封面、19 个作者，3 个压缩清单各为 67 项。历史分类表中的“弃用 76”仅代表清理前快照。
- 线上清理前备份保留在 `/home/ubuntu/backups/dragonReserveSystem/current-before-cover-cleanup-20260925T021348`，含数据库、当时的 storage、后端代码和配置以及未纳入 Git 的弃用资源；文件 SHA-256 已核验。迁移前数据库备份 `/home/ubuntu/apps/dragonReserveSystem/backend/migration_backups/dragon_reserve-before-0019-20260923-035808-no-tablespaces.sql` 也保留。
- 清理完成、后端重启且健康接口和封面 API 验证通过后，删除了三个指定旧备份：`pre-release-20260906T170225Z`、`storage/cover-deploy-backups/20260907T110202Z`、`migration_backups/backend-code-before-6400492.tar.gz`；其他备份未动。
- 本地资产与清单变更尚未提交或推送。后续部署需保持目录和文件同步；旧文件仍可能存在于 Git 历史中，本次清理并不改写历史。
## 后续新增素材归档规则

- 自 2026-09-22 起，用户新增的活动封面按**分类**归档，不再按画师或人名创建目录。
- 新文件统一放入 `backend/app/assets/activity-covers/categories/<分类>/`；封面 ID 仍保持稳定，分类以 `categories` 字段为准。
- 用户随图片提供的人名/署名必须写入封面的 `credit_name` 元数据；工作台会展示并支持按封面 ID、署名和历史作者查找。`credit_name` 不作为目录层级或分类依据。
- 此规则只适用于后续新增素材；既有按画师目录保存的历史封面不做迁移，避免改变已发布资源路径。

