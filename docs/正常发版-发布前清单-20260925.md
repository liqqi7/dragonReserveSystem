# 正常发版：这次要做什么（2026-09-25）

**当前状态：后端已部署并恢复服务，历史分享图已回填；小程序仅生成真机预览包，尚未上传或正式发布。** 前端真机视觉和微信分享收发仍待验收，以下步骤不能以代码检查代替。

1. **确认包内容。** 本次前后端、头像弹窗 SVG、分享图标与迁移/回填脚本一起发布；用户主动删除的封面文件也一并提交。选择性检查并暂存源码与这些删除，排除 `.codex-tmp/`、`backend/storage/`、本地演示图、`prototype/` 未追踪图片和本机环境配置。提交前查看暂存清单及体积，不能直接 `git add -A`。
2. **先做线上备份与资产核对。** 备份生产数据库、现有封面素材和 `backend/storage/`（尤其头像、分享图），确认备份可恢复；核对当前线上活动引用的旧封面 ID 及新封面素材是否齐全。迁移 `20260923_0019` 会将部分旧 ID 合并，**不能靠 Alembic downgrade 还原**，回退需要迁移前的数据库和素材备份。
3. **后端先发。** 暂停/隔离活动写入，安装新依赖（含 CairoSVG 所需原生库）、执行 `alembic upgrade head`（0019、0020），部署后端代码和封面删除，检查服务健康、错误日志、生产媒体目录写权限、HTTPS 图片访问与现存封面引用；恢复写入前完成这些检查。绝不能用仓库空的 `backend/storage/` 覆盖线上媒体。
4. **补齐历史分享图。** 在生产后端目录先执行 `python -m scripts.backfill_activity_share_previews --limit 100` 干跑，再按确认过的批次使用 `--apply --start-after <上一批末尾ID> --limit 100`。逐批核对失败数为 0、图片可读取且公网 URL 可访问；失败先修复并重试。旧分享图片**暂不清理**。
5. **最后上传小程序。** 确认被忽略的 `miniprogram/services/config.js` 实际打包地址是生产 HTTPS，后端及历史分享图均就绪后，才上传并发布前端包。真机验收登录/头像弹窗、创建与编辑活动（封面、地点、时间）、立即分享及接收卡片，并抽查报名、签到；不能用单测或开发者工具代替微信真机分享验收。

**停止/回滚条件：** 备份未确认、迁移失败、缺封面、回填失败、公网分享图打不开或真机流程有问题时停止下一步。前端未发可保持原版；后端需要回退时保留新媒体并按迁移前备份恢复数据库及对应封面，不能直接对 0019 执行 downgrade。本次 GitHub 推送与后端部署已按用户确认的发版范围执行；小程序上传/正式发布和线上旧封面物理清理以真机验收及资产引用核对为门槛。

## 2026-09-25 本轮执行记录（香港时间）

- GitHub 发版分支已推送 `50a49a1`（含修改个人资料弹窗的全视口居中修复）。本地仍有未追踪的临时/演示文件，未打包进小程序。
- 发布前一致性备份：服务器 `backend/migration_backups/release-20260925-155014/`，数据库 SQL、后端代码与 `.env`、完整 `backend/storage` 的校验均通过。部署使用预备包但不执行 `rsync --delete`，不覆盖 `.env`、`storage`、线上独有素材或本地数据库文件。
- 生产后端已同步并执行 `20260924_0020`，已恢复后端和两个定时任务；公网健康接口正常。服务器原有 `APP_ENV` 未配置、运行值为 `development`，本次未擅自改动。
- 57 个历史活动分享图回填 `processed=57 failed=0`；57 个数据库引用、PNG 文件、公网分享接口和 HTTPS 图片响应均逐一通过。旧版/新版活动列表都返回 57 个活动；分类封面示例图片返回 HTTP 200。
- 自动化：小程序 532/532、后端 145/145（49 条弃用警告）、Skyline WXSS 0 错误；开发者工具登录有效，正式环境真机预览包生成成功（约 1.1 MB），二维码在 `.codex-tmp/release-preview-20260925.png`。
- **仍待完成：** 真机检查修改资料/报名弹窗的整屏居中、创建/编辑与分享收发；确定小程序上传版本号并上传/提交审核/正式发布。Git 中用户删除的旧封面已随代码提交，但服务器物理旧文件暂留作缓存兼容与回退，不宣称线上素材已清理。
