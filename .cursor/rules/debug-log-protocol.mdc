---
description: 调试日志基础设施使用规范：日志路径、读取方式、禁止行为
alwaysApply: true
---

# 调试日志协议

## 日志文件路径

Cursor 内置调试服务器（7776 端口，由 Cursor.exe 持有）将日志写入：

```
{工作区根目录}/.cursor/debug-{sessionId}.log
```

本项目当前 session 日志：`D:\VibeCoding Project\dragonReserveSystem\.cursor\debug-01549b.log`

## 禁止行为

- ❌ 不得要求用户手动复制/粘贴日志内容——日志文件可直接用 Read 工具读取
- ❌ 不得在项目根目录或其他位置查找日志文件，只在 `.cursor/` 子目录找
- ❌ 不得因 PowerShell `Invoke-RestMethod` 返回 400 就判断服务器故障——PowerShell 测试需带 `X-Debug-Session-Id` 头，且 Cursor 调试服务器只接受 `wx.request`（小程序）写入，GET 端点不存在
- ❌ 不得使用 `fetch` 替代 `wx.request` 发送调试日志——违反用户规则，且 `wx.request` 本身就能正常写入

## 正确流程

1. **写入**：埋点用 `wx.request` POST 到 `http://127.0.0.1:7776/ingest/{runId}`，必须带 `X-Debug-Session-Id` 头
2. **读取**：直接 `Read(".cursor/debug-01549b.log")` 或用 `Grep` 搜索关键字
3. **清空**：每次新的调试轮次前用 `Delete` 工具删除日志文件，不用 shell 命令
4. **验证服务器**：用带正确头的 PowerShell 测试 —— `Invoke-RestMethod ... -Headers @{'X-Debug-Session-Id'='01549b'}`，成功返回空响应（200）

## 事件未触发 ≠ 日志丢失

如果日志里没有某类事件（如 H2 横向 dx、H3 setData 延迟），先确认**用户是否实际执行了对应操作**，不要误判为日志系统故障。
