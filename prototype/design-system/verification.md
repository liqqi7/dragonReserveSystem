# 2026-09-13 统一规范更新验证

## 更新范围

本轮比较用户附件与现有桌游功能稿，更新 AGENTS.md、统一规范/来源快照/tokens、补全 Pencil 生成器及交互预览。现有主原型、附件原文件、数据库、后端业务逻辑和小程序页面代码未在本轮改动。

## 已执行检查

| 检查 | 实际结果 |
| --- | --- |
| `.pen` 生成与全画布几何 | 33 个顶层对象、30 个页面/状态、1060 个源节点；全部顶层碰撞 0、子节点越界 0 |
| 分区排列 | 三个组件区同排，七个业务模块分行；业务横向间距 80、纵向间距 156；组件与业务间距 156 |
| 组件与引用 | 导航、按钮、两类抽屉、馆藏卡共五个复用模板；实例覆盖与引用解析通过 |
| 字号与素材 | 源节点与展开实例均满足七档字号；7 处图片引用加载成功 |
| 浏览器全画布文字 | 所有页面可见时测量文字盒子，横向/纵向溢出均为 0；32px 数字保留40px实际行高，并给文字盒子额外容纳字形空间 |
| 抽屉实测 | 信息抽屉360高、内容284；表单558高、内容402、操作栏80；两者标题区40、关闭入口32，灰底一致 |
| 320px 内容宽度 | 列表、活动、录局、选择玩家、特殊状态及赢家表单控件无横向越界；确认实际 root 宽度为320px |
| 提名 / 记录 | 加入提名后3人变4人；从活动进入录局保留活动；小陈0分保持，小赵摆烂分数禁用、不可被选为赢家 |
| 表单错误 | 错误提示出现在可滚动内容区，标题区和80px底栏尺寸保持；关闭入口与取消操作可用 |
| 代码与数据 | Python源/JSON解析、预览JS语法检查、生成器重复执行与嵌入同步检查通过 |

截图保存在 [补全原型 QA](../boardgame-library/qa/)：全画布、基础规范、通用组件、业务卡片、馆藏列表、活动、记分、状态抽屉、库存表单、计分纸、详细统计、导入公开和窄屏错误提示。

规范中的720px最高档由共享设计值、模板规则与高度选择逻辑定义；本轮实际交互覆盖360和558档，不把未触发的最高档描述为真机验证完成。

## 验证边界

截图来自同一份节点数据的浏览器渲染，未使用 Pencil 原生渲染器；Pencil 实开效果仍需核对。小程序旧样式本轮未迁移，微信开发者工具/iOS/Android的键盘、原生控件和安全区不属于已验收内容。

这是本地规范与原型更新，无数据库迁移或线上部署步骤。需要回退时恢复本轮规范和原型文件及 AGENTS.md 对应章节即可，不应回退此前已开发的业务代码或用户其他修改。
